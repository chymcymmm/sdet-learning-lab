# SDET Learning Lab Daily Protocol

## 每天只记住一条线

```text
学一个知识
 ↓
放进真实测试
 ↓
做一个判断
 ↓
留下一个产物
```

这条线每天都包含技术、测试和实际工作能力。

---

## 01｜每天的固定动作

### ① 学一个基础知识

一次只抓一个主要知识点。

例如：

**HTTP Header / Python 字典 / SQL JOIN / Requests POST / Pytest Fixture / Jenkins Pipeline**

回答三个问题：

> 它是什么？  
> 它解决什么问题？  
> 测试里怎么用？

---

### ② 用真实项目马上练

优先使用 Ruffwear 小程序 API。

当天知识点直接落到一个接口或业务场景。

例如学 `Header`：

```text
分析接口 Header
 ↓
Token 正常
Token 缺失
Token 错误
Token 过期
 ↓
观察实际结果
```

---

### ③ 做一次测试判断

不是只“跑接口”。

至少判断：

**正常 · 异常 · 边界 · 权限 · 数据状态**

复杂业务再增加：

**重复请求 · 幂等 · 并发 · 异步 · 数据一致性**

---

### ④ 做一次沟通练习

从当天测试中找一个真实问题，写一句完整表达。

例如：

> 当前接口文档定义 token 从 Header 传递，但实际调用还存在 Query 传参，想确认最终以哪种方式为准。

每天只需要一条。

---

### ⑤ 留下一个产物

当天产生什么，就沉淀什么：

| 情况 | 产物 |
|---|---|
| 学 API | API 分析 |
| 学测试 | 测试用例 |
| 发现问题 | Bug / 风险记录 |
| 学 SQL | SQL + 校验结果 |
| 学 Python | Python 代码 |
| 学自动化 | Pytest 用例 |
| 做项目 | 测试计划 / 排期 / 报告 |
| 学 Jenkins | Pipeline / Jenkinsfile |

---

# 02｜测试计划、排期、报告怎么融入日常

不要求每天写一份完整文档，而是在真实练习中不断补齐。

### 测试计划

第一次只写：

**测什么 · 为什么测 · 重点风险 · 什么时候测**

之后逐步加入：

**范围 · 策略 · 环境 · 数据 · 资源 · 进入 / 结束条件**

### 测试排期

从：

```text
开发 → 提测 → 测试 → 回归 → 发布
```

逐步增加：

```text
需求评审 → 开发 → 提测 → 冒烟
→ 功能 / API → 回归 → 验收 → 发布
```

### 测试报告

第一次只写：

**测了什么 · 结果如何 · 有什么问题 · 风险是什么**

之后再完善版本、环境、执行情况、缺陷统计和发布建议。

---

# 03｜忙的时候怎么办

最低标准只有 4 项：

```text
□ 一个基础知识
□ 一个真实接口 / 场景
□ 一个测试判断
□ 一条沟通记录
```

完成这四项，当天就算完成。

---

# 04｜正常的一天

如果时间充足：

```text
知识学习
 ↓
接口分析
 ↓
测试设计
 ↓
Apifox 执行
 ↓
Python / SQL
 ↓
自动化
 ↓
问题记录
 ↓
沟通
 ↓
测试结论
 ↓
Git 提交
```

不要求每天把所有技术都碰一遍。

核心是：

> **当天学到什么，当天就让它进入测试场景。**

---

# 05｜每周完成一次完整闭环

每周至少挑一个接口或业务模块，完整走一遍：

```text
需求
 ↓
澄清
 ↓
风险
 ↓
测试计划
 ↓
测试排期
 ↓
测试设计
 ↓
执行
 ↓
缺陷沟通
 ↓
回归
 ↓
测试报告
```

技术能力提升后，把：

**Python / Requests / Pytest / SQL / Jenkins**

逐步接进这条链路。

---

# 06｜GitHub 记录原则

每周持续产生小提交即可。

例如：

```text
docs: 新增 HTTP Header 学习笔记
test: 新增会员接口异常场景
sql: 新增积分数据校验
feat: 新增接口自动化测试
ci: 新增 Jenkins Pipeline
```

重点不是提交多少，而是仓库持续留下真实成长记录。

---

# 07｜最终学习判断

不要用“课程看完”判断自己是否学会。

每个知识点最终都问自己：

```text
我能解释吗？
我能测试吗？
我能发现问题吗？
我能说明风险吗？
我能沟通吗？
我能留下产物吗？
```

能完成，就继续往下。

不能完成，就把这个知识点再次放回真实项目练一遍。
